version: 0.2
run-as: ubuntu
phases:
  install:
    runtime-versions:
      nodejs: 12
  pre_build:
    commands:
      - cd site
      - npm ci
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo "post build"
